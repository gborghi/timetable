[pytest]
testpaths = tests
addopts = -ra
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: integration tests that invoke the full CP-SAT solver pipeline; run with `-m slow` to include them, or `-m "not slow"` to skip.
