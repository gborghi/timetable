fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
sqlalchemy>=2.0
sse-starlette>=2.0
python-multipart>=0.0.9
openpyxl>=3.1
reportlab>=4.0
ortools>=9.7
numpy>=1.24
scikit-learn>=1.3
faker>=24.0
# Diagnostics + analytics (new tab Statistiche e diagnostica)
networkx>=3.0           # bipartite graph metrics
statsmodels>=0.14       # regressions + p-values (correlations module)
pandas>=2.0             # tabular helpers for diagnostics

# Migration tool (Section 2.7 P1).
alembic>=1.13

# Optional Postgres driver (Section 2.5 P2). Default DB is SQLite; install
# this only when switching to Postgres via PITANTUM_DB_URL.
# psycopg[binary]>=3.1

# Optional async DB drivers (Section 2.5 P2 async). Required only for
# the AsyncSession layer wired in services/async_session.py.
# aiosqlite>=0.19      # for sqlite+aiosqlite:// URLs
# asyncpg>=0.29         # for postgresql+asyncpg:// URLs

# Dev / test extras (optional but pulled by `pip install -r requirements.txt`)
pytest>=8.0
httpx>=0.27
