# Runtime dependencies for the FastAPI backend.
#
# Versions are constrained with `>=,<` ranges instead of bare `>=` so
# upstream breaking changes don't silently drift into every fresh
# install. For fully reproducible builds, generate a lockfile with
# `pip-compile requirements.txt -o requirements.lock` and pin from
# the lock in containers / production.
#
# Dev + test tooling is in `requirements-dev.txt` -- install both for
# local development:
#
#   pip install -r requirements.txt -r requirements-dev.txt

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
sse-starlette>=2.0,<3.0
python-multipart>=0.0.9,<1.0

# Database
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0

# Document / spreadsheet export
openpyxl>=3.1,<4.0
reportlab>=4.0,<5.0

# Optimisation engine
ortools>=9.7,<10.0

# Numerical stack
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
networkx>=3.0,<4.0
statsmodels>=0.14,<1.0
pandas>=2.0,<3.0
faker>=24.0,<40.0

# Optional Postgres driver (uncomment when switching off SQLite via
# PITANTUM_DB_URL). Kept commented so a default `pip install -r ...`
# stays SQLite-only and doesn't require libpq.
# psycopg[binary]>=3.1,<4.0

# Optional async DB drivers (required only when wiring up the
# AsyncSession layer in services/async_session.py).
# aiosqlite>=0.19,<1.0
# asyncpg>=0.29,<1.0
