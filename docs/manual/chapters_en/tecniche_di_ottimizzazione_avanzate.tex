\chapter{Advanced optimization techniques}
\label{ch:tecniche_avanzate_en}

This chapter goes into the \emph{advanced mathematical depth}
of selected techniques. The base metaheuristics (LNS, ALNS, SA,
TS, ILS, VNS), the Hall pre-check, and the general idea of
Lagrangian relaxation and column generation are already covered
by their dedicated chapters in the Italian edition: see
\texttt{metodo\_metaeuristiche.tex} for the six metaheuristics,
\texttt{metodo\_hall.tex} for the Hall pre-check, and
\texttt{metodo\_lagrangian.tex} for Lagrangian relaxation and
basic column generation. The four Phase~B decomposition
alternatives (spectral, temporal, curriculum, metis) and the
selection criteria live in \texttt{metodo\_spettrale.tex}; the
two pipeline switches \texttt{cp\_sat\_scope} and
\texttt{phase\_a\_mode} are covered in
\texttt{workflow\_di\_ottimizzazione.tex}.

What remains here are the three deep dives that have no home
elsewhere: the formal \texttt{DayCountModel} for Phase~A; the
internal anatomy of one Branch-and-Price iteration (master LP,
nine pricer granularities, Ryan-Foster, dual stabilization,
EWMA column management, parallel pricing, pricing-in-nodes); and
the \emph{tightness} criterion for deciding how much
metaheuristic exploration is worth running after Phase~B.

\section{Phase A: the formal \emph{day count} model}
\label{sec:phase-a-formal-en}
\index{Phase A}\index{day count}

Phase A solves a \emph{distribution} problem: given the weekly
demand of every cattedra (typically 2--6 hours), over how many
days and with what daily load does each teacher work? The
formulation is a small CP-SAT on integer variables
\texttt{dc[t,c,s,d]} (\emph{day count}: hours of $(t,c,s)$ on
day $d$).

\begin{defbox}[title={The \texttt{DayCountModel}}]
Let $T$ be teachers, $C$ classes, $S$ subjects,
$D = \{1,\dots,6\}$ days. For each cattedra $(t,c,s) \in
\mathit{Catt}$ with weekly demand $h_{t,c,s} \in \mathbb{N}_+$
and each $d \in D$:
\[
  dc_{t,c,s,d} \in \{0, 1, \dots, h_{\max}\},\quad h_{\max}=6,
\]
subject to
\begin{align}
\text{(coverage)}\quad &\sum_d dc_{t,c,s,d} = h_{t,c,s} & \forall (t,c,s) \\
\text{(class daily load)}\quad &\sum_{(t,s)} dc_{t,c,s,d} \in \{0\}\cup [4,6] & \forall c, d \\
\text{(teacher daily load)}\quad &\sum_{(c,s)} dc_{t,c,s,d} \le h^{\text{prof}}_{\max} & \forall t, d
\end{align}
The objective combines: deviation from the ideal load
(weight\,4), weekly uniformity (weight\,3), fragmented free-day
penalty (weight\,1).
\end{defbox}

The architectural turn of Step 4: the same \texttt{DayCountModel}
that produces day counts in Phase~A can be \emph{reconstructed}
from the weekly solver (\texttt{cp\_sat\_scope=week}); the two
sources of \texttt{dc\_value} become comparable, and the cost
of Phase~A's rigidity becomes measurable.

\subsection{Migration from hardcoded to DSL pragmas}

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{p{4cm}p{8cm}}
\toprule
Pragma & Constraint emitted \\
\midrule
\texttt{class\_day\_load\_in\_day\_count} &
  for each $(c,d)$, $\sum_t dc_{t,c,*,d} \in \{0\} \cup [4,6]$ \\
\texttt{free\_day\_choice\_3way} &
  every teacher gets at most ONE of \{free day, half-day,
  full week\} \\
\texttt{hall\_bound\_prof\_day} &
  Hall-style bound: $\sum_{c,s} dc_{t,c,s,d} \le K_t$ \\
\texttt{subject\_day\_count\_in} &
  for cattedra $(t,c,s)$, allowable counts
  $dc_{t,c,s,d} \in \{0,1,2\}$ \\
\texttt{subject\_day\_count\_pair} &
  two correlated subjects (e.g.\ Maths+Sci) on the same day \\
\bottomrule
\end{tabular}
\caption{The five Phase~A pragmas. All emit CP-SAT bytes
identical to the legacy \texttt{solve\_phase\_a}
(zero-drift); migration is tracked in
\texttt{webui/backend/tests/test\_dsl\_intvar\_pragmas.py}.}
\label{tab:phase-a-pragmas-en}
\end{table}

\section{Phase B: the Cheeger bound on bridges}
\label{sec:phase-b-decomp-bound-en}
\index{Phase B}\index{Cheeger (bound)}

The four Phase~B decomposition alternatives (spectral,
temporal, curriculum, metis) and the operational selection
criteria are treated in the spectral decomposition chapter
(Italian edition, \texttt{metodo\_spettrale.tex}, section
``Three alternatives to spectral decomposition''). What remains
here is the \emph{quantitative} result that explains
\emph{why} spectral typically produces fewer bridges than its
competitors: the link between the second Laplacian eigenvalue
and the number of inter-cluster edges.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{manual/figures/fig_decomp_methods}
\caption{Phase~B wall-clock per decomposition method, four
scales. Monolithic times out on \texttt{superhuge}; the four
decompositions converge in comparable time, with a slight edge
for \texttt{spectral} and \texttt{metis} on the larger
regimes. Log scale to fit the dispersion small$\to$superhuge.}
\label{fig:decomp-methods-en}
\end{figure}

\begin{proposition}
Let $G = (V, E)$ be the teacher co-occupation graph (edge if
two teachers share at least one class). Spectral decomposition
yields $k$ clusters $C_1, \dots, C_k$ with
$|E(C_i, C_j)| \le \epsilon |E|$, $\epsilon$ proportional to
the second eigenvalue $\lambda_2(L_{\mathrm{sym}})$.
Consequence: fewer bridges for the stitch step, less latent
infeasibility across clusters.
\end{proposition}

\begin{proof}
The classical Cheeger bound~\parencite{chung1997} on the
normalized cut $h(G) \ge \lambda_2(L_{\mathrm{sym}})/2$ implies
that, picking clusters as the level sets of an eigenvector
associated with $\lambda_2$, the number of edges cut is at most
$O(\sqrt{\lambda_2 \cdot |E|})$. For a school graph
$\lambda_2$ is typically small (well-separated natural clusters),
so the number of bridges is reduced. See the Italian
\texttt{metodo\_spettrale} chapter for the full derivation of
the Fiedler vector and the bound.
\end{proof}

\section{\texttt{cp\_sat\_scope=week}: exercise numbers}
\label{sec:scope-week-numbers-en}
\index{cp\_sat\_scope}\index{MonolithicSolver}

The \texttt{cp\_sat\_scope=week} mode is described in detail in
the optimization workflow chapter (\texttt{workflow\_di\_ottimizzazione.tex},
section ``CP-SAT scope and Phase~A mode''). What stays here are
the \emph{sizes} observed in production, useful for sizing RAM
and time budget.

\begin{statsbox}
\begin{itemize}
\item Boolean variables on \texttt{huge}: $\sim$\,$1.5 \times 10^5$
\item Boolean variables on \texttt{mega}: $\sim$\,$3.5 \times 10^5$
\item Peak RAM (mega, 8 workers): $\sim$\,$11$\,GB
\item Median wall (mega): $\sim$\,$15$\,min within soft target;
      $30$\,min hard cap in production.
\item \texttt{phase\_a\_mode=soft\_hint}: typically $-15\%$
      final SOFT vs \texttt{always}, $+30\%$ wall.
\end{itemize}
\end{statsbox}

\section{Branch-and-Price: anatomy of one iteration}
\label{sec:bp-anatomy-en}
\index{Branch-and-Price}

The basic idea of column generation (master LP + pricer
generating columns from dual prices) is introduced in the
Lagrangian chapter (Italian edition,
\texttt{metodo\_lagrangian.tex}, section ``Column Generation: the
real-estate catalogue''). What follows is the detail of
\emph{how} \texttt{piTantum} implements the full version with all
the scalability techniques required for MEGA instances: the
master structure, the nine pricer granularities, and the five
scalability techniques composed around the loop.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{manual/figures/fig_bp_components}
\caption{Five composable scalability techniques around the
\emph{Branch-and-Price} loop: Ryan-Foster recursive tree,
box-step dual stabilization, column management, parallel
pricing, pricing-in-nodes (Step~4). All five are on by default
in \texttt{mode="branch-and-price"}.}
\label{fig:bp-components-en}
\end{figure}

The modern formulation of Branch-and-Price unifies
\emph{column generation} and \emph{branch-and-bound} under one
framework~\parencite{barnhart1998,vanderbeck2000}.
Dantzig-Wolfe decomposition~\parencite{dantzigwolfe1960} is
its backbone: the original problem is rewritten in terms of
\emph{patterns} satisfying local constraints (subproblem),
and the \emph{master} picks a convex combination meeting the
global ones. Desaulniers, Desrosiers and
Solomon~\parencite{desaulniers2005} remain the applied
reference.

\subsection{Master LP (Dantzig-Wolfe)}

\begin{align}
\min \quad & \sum_{j \in \mathcal{P}} c_j\, \lambda_j \\
\text{s.t.} \quad & \sum_j a_{ij}\, \lambda_j \ge b_i & \forall i \in \text{cover}\\
& \lambda_j \ge 0
\end{align}

$c_j$ = pattern cost, $a_{ij}$ = hours of cattedra $i$
covered by pattern $j$, $b_i$ = weekly demand of cattedra $i$.
The dual vector $\pi$ guides the pricer.

\subsection{CP-SAT pricer: nine granularities}

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{p{3.4cm}p{4.0cm}p{4.0cm}}
\toprule
Granularity & Column encoding & When \\
\midrule
\texttt{teacher}                & weekly orario of one teacher                & small schools, strong teacher constraints \\
\texttt{teacher-day}            & orario of one (teacher, day)                & tight daily constraints \\
\texttt{teacher-class}          & orario of a cattedra (t,c)                  & per-cattedra coverage \\
\texttt{teacher-class-subject}  & orario of a (t,c,s) cattedra-slot           & medium schools, support+curriculum mixing \\
\texttt{teacher-subject}        & orario of (t,s) across classes              & multiple cattedre on the same subject \\
\texttt{class}                  & weekly orario of one class                  & class-side hard constraints (no holes) \\
\texttt{class-day}              & orario of (class, day)                      & daily class constraints (h3 at 11) \\
\texttt{day}                    & full daily orario                           & explicit canonical Phase B in BP form \\
\texttt{curriculum}             & orario of a curriculum (Scientific, ...)    & large schools, multi-curriculum \\
\bottomrule
\end{tabular}
\caption{Nine pricer granularities. Each defines what a pattern
\emph{is} and therefore the CP-SAT model the pricer builds.
None dominates the others.}
\label{tab:bp-granularity-en}
\end{table}

For every iteration the pricer receives the master duals $\pi$
and solves a CP-SAT with reduced-cost objective
$\bar{c}_j = c_j - \pi^\top a_j$. If $\bar{c}_j < 0$, column $j$
enters the master; when no pricer finds a negative column the
iteration terminates.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{manual/figures/fig_bp_iter_progress}
\caption{Master LP soft-cost convergence per granularity
(\texttt{small} synthetic profile). Finer granularities
(\texttt{curriculum}, \texttt{teacher-class-subject}) reach
soft cost $0$; coarser ones plateau because patterns cannot
change enough. Iter=1 \texttt{no\_improving\_column} runs
correspond to seed catalogues already optimal.}
\label{fig:bp-iter-progress-en}
\end{figure}

\subsection{\texttt{PhaseBDaySolver}: OO wrapper around the legacy}
\label{sec:bp-mvp-scaffold-en}

A practical difficulty in the Phase~B refactor toward BP is
that the legacy \texttt{solve\_phase\_b\_for\_day} function is
covered by 100+ regression tests: a from-scratch rewrite risked
breaking subtle behaviours that the tests captured by
construction. Step~4 solves this with an \emph{OO wrapper} that
exposes the object-oriented interface required by the new BP
(\texttt{solve()}, scope access, \texttt{via\_dsl} augmentation)
but delegates model construction to the proven legacy code.
Consequence: 100+ regression tests keep passing \emph{by
construction}; with \texttt{via\_dsl=True} the legacy model is
augmented with extra DSL constraints (DB-stored rules + extra
expressions passed to the constructor). The smoke test
\texttt{tests/benchmarks/test\_bp\_step4\_smoke.py} runs the
full loop (Mono $\to$ initial pool $\to$ master DW $\to$ RF
tree $\to$ integer recovery $\to$ completion) on a
\texttt{small} scenario (10 classes) and a \texttt{medium}
one (28 classes), checking HARD-feasibility and that the soft
cost does not regress against the \emph{iterative-diversified}
baseline.

\subsection{Ryan-Foster recursive tree}

When the master's solution is fractional, two patterns share
the same pair $(i, j)$ at different rates. Ryan and
Foster~\parencite{ryanfoster1981} pick the active pair with
maximum Achterberg score~\parencite{achterberg2005} (fraction
$\times$ RHS contribution) and create two children:
\textbf{together} ($\lambda$-patterns must include both hours)
and \textbf{apart} (must exclude one).

\begin{schemabox}[title={Pseudocode -- Ryan-Foster recursive tree}]
\sffamily\small
\textbf{procedure} \texttt{rf\_branch}($\text{pool}$, $\text{depth}$):\\
\hphantom{XX} if $\text{depth} > $ MAX\_DEPTH ($=20$): \textbf{return} best\_int\\
\hphantom{XX} solve master LP on $\text{pool}$ $\to$ $\lambda^*, \pi^*$\\
\hphantom{XX} if $\lambda^*$ integral: \textbf{return} $\lambda^*$\\
\hphantom{XX} pick $(i_*, j_*)$ with max Achterberg score\\
\hphantom{XX} $\text{pool}_T \gets \texttt{branch\_together}(\text{pool}, i_*, j_*)$\\
\hphantom{XX} $\text{pool}_A \gets \texttt{branch\_apart}(\text{pool}, i_*, j_*)$\\
\hphantom{XX} return best of \texttt{rf\_branch}($\text{pool}_T$, $\text{depth}+1$),\\
\hphantom{XXXX} \texttt{rf\_branch}($\text{pool}_A$, $\text{depth}+1$)
\end{schemabox}

\begin{theorem}[Branch-and-Price termination]
If CP-SAT pricers always terminate (column found or proved
absent) and Ryan-Foster has a finite depth cap, the BP loop
terminates in finite time with an integer solution optimal for
the cover relaxation.
\end{theorem}

\subsection{Box-step dual stabilization}

Master duals oscillate between iterations (degeneracy). The
box-step scheme keeps a smoothed
$\pi_{\text{stable}}$ and feeds the pricers
$\pi_{\text{blend}} = (1-\alpha)\pi_{\text{stable}} +
\alpha \pi_{\text{raw}}$ with $\alpha = 0.2$. Enough to reduce
by $40$--$60\%$ the number of iterations in which the pricer
returns a false-positive ``no improving column''.

\subsection{EWMA column management}

After $50$ iterations a \texttt{teacher} pricer hits
$\sim 3000$ columns; $80\%$ are inactive. \texttt{piTantum}
keeps a $20$-iter EWMA of each column's reduced cost and, when
the pool exceeds $10\,000$, purges the worst while preserving
(a) columns active in the master basis and (b) seed columns.
Steady-state pool size: $\sim 8\,000$ columns.

\subsection{Parallel pricing}

CP-SAT pricers across granularities are independent: the BP
loop runs them in parallel via \texttt{ProcessPoolExecutor}
with \texttt{workers = $\lfloor$cpu\_count() / 2$\rfloor$}. The
\texttt{cpu/2} split is empirical: leaving half the CPU to the
master LP reduces context switching. \texttt{ThreadPoolExecutor}
is not an option here: the OR-tools binding releases the GIL
only partially, and CP-SAT pricing is CPU-bound.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{manual/figures/fig_bp_granularity_comparison}
\caption{Pricing time per granularity, with \texttt{dc\_source}
between Phase~A (rigid) and weekly (\texttt{cp\_sat\_scope=week}).
Small scenario shows no practical difference.}
\label{fig:bp-granularity-en}
\end{figure}

\subsection{Pricing-in-nodes (Step 4)}

Step 4 introduces \emph{pricing-in-nodes}: at each RF tree
node the pricer is re-launched with branch decisions
(\texttt{together}/\texttt{apart}) already applied to its
CP-SAT model. The benefit: search space at each depth shrinks
\emph{structurally}, and the quality of generated columns
grows.

\subsection{Measured numbers}

\begin{statsbox}
Synthetic MEGA benchmark
(\texttt{tests/benchmarks/test\_bp\_huge\_mega.py}):
\begin{itemize}
\item MEGA scenario (100 classes, $\sim$174 teachers, 4
  subjects, $\sim$400 cattedre): HARD-feasibility in
  \textbf{5.95\,s} with \texttt{granularity=curriculum} and all
  scalability techniques enabled.
\item HUGE scenario (50 classes, $\sim$95 teachers, 200
  cattedre): \textbf{2.57\,s}.
\item The synthetic scenario is structurally easy (cattedre as
  4-hour single-day blocks); on real instances with arbitrary
  day distributions the calibration target stays at 30 minutes
  for MEGA, with a 60-minute hard cap.
\end{itemize}

MEGA real (100 classes, 178 teachers, 2653 cattedra-day; total
wall 1493\,s):
\begin{itemize}
\item Phase~A: 301\,s (20\%)
\item BP loop (5 iterations): 1192\,s (80\%)
\item Final soft cost: 530
\item Final pool: 2075 columns
\end{itemize}
\end{statsbox}

\begin{figure}[h]
\centering
\includegraphics[width=0.78\linewidth]{manual/figures/fig_mega_real_breakdown}
\caption{MEGA real (100 classes, 178 teachers, 2653 cattedra-day):
breakdown of total wall-clock 1493\,s into Phase~A
(\texttt{301}\,s) + Branch-and-Price (\texttt{1192}\,s) + post
(\texttt{<0.1}\,s).}
\label{fig:mega-real-breakdown-en}
\end{figure}

\section{The tightness criterion: how much metaheuristic exploration is worth running}
\label{sec:meta-tightness-en}
\index{tightness}\index{metaheuristics}

The six metaheuristics (LNS, ALNS, SA, TS, ILS, VNS) and their
typical contexts are described in the Italian
\texttt{metodo\_metaeuristiche.tex} chapter. What remains here
is the quantitative criterion for deciding, before launching,
\emph{how much} metaheuristic budget is worth spending on an
instance. \texttt{piTantum}'s answer is the
\emph{tightness score}.

The canonical treatment of ALNS is Pisinger and
Ropke~\parencite{pisinger2010,ropke2006_orig}; the surveys of
Burke and Petrovic~\parencite{burke2002} and
Pillay~\parencite{pillay2014} are the standard references for
educational timetabling at large.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{manual/figures/fig_tightness_band}
\caption{Total time vs scenario tightness for the three
dominant combinations (Phase~B alone, $+$\,SA, $+$\,ALNS).
The ivory band is the metaheuristic spread, growing with
tightness.}
\label{fig:tightness-band-en}
\end{figure}

\texttt{piTantum} computes a \emph{tightness score} that
measures how tight the available slots are versus demand:
\[
  \text{tightness} = \frac{\sum_t h_t}{|T| \cdot 6 \cdot 6 \cdot \rho},
\]
where $\rho \in [0,1]$ is the average per-teacher slot
availability after applying unavailabilities. Empirical guide:
\begin{itemize}
\item tightness $< 0.4$: Phase~B alone, no metaheuristics.
\item tightness $\in [0.4, 0.6]$: $+$\,SA, $\sim 10\%$ SOFT win.
\item tightness $> 0.6$: $+$\,ALNS necessary; cascade SA $\to$
  ALNS $\to$ TS, $5$\,min budget.
\item tightness $> 0.8$: likely HARD-infeasibility; pre-flight
  Hall check mandatory.
\end{itemize}
