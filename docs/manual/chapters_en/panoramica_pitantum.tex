\chapter{piTantum overview}
\label{ch:panoramica_pitantum_en}

Three-tier architecture: engine (CP-SAT solver core), backend (FastAPI + SQLite), frontend (SvelteKit). Pipelines exposed as async runs via /api/optimize/*. The 16 UI tabs covering registry, constraints, generation, manual editing, daily operations.

\medskip
\noindent
\textit{This chapter is an English summary of the corresponding Italian chapter. The full text remains in the Italian edition (\texttt{docs/manual/chapters/panoramica_pitantum.tex}). For the implementation references see the engine modules in \texttt{engine/} and the API documentation at \texttt{docs/api.md}.}
