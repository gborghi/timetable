\chapter{Benchmark e risultati}
\label{ch:benchmarks}

\epigraph{``In God we trust. All others must bring data.''}{W.\,E.
Deming, attribuito}

\lettrine[lines=3]{Q}{ualunque} sistema di ottimizzazione si
giudica anche e soprattutto dai numeri che produce. Questa
sezione raccoglie i risultati misurati di $\pi$Tantum sui
sei profili di test, con il triplice obiettivo di:
documentare lo stato dell'arte; permettere a chi voglia
estendere il sistema di confrontare la propria modifica
contro un baseline; mettere il lettore in condizione di
sapere se $\pi$Tantum \`e adatto al \emph{suo} caso.

\section{I sei profili}

\sidenote{I profili sono generati deterministicamente con
seed fissi e Faker, dal modulo
\texttt{webui/backend/mock\_school.py}. Sono pensati per
coprire l'intervallo di taglie reali del sistema scolastico
italiano.}

I profili sono:

\begin{itemize}
\item \texttt{small}: 8 classi, 17 docenti, $\sim$220 studenti,
  17 aule, 8 indirizzi. Una scuola piccola di provincia.
\item \texttt{medium}: 28 classi, 50 docenti, 700 studenti,
  35 aule. Un istituto medio.
\item \texttt{big}: 40 classi, 75 docenti, 1000 studenti,
  50 aule. Un istituto grande.
\item \texttt{huge}: 50 classi, $\sim$95 docenti, 1250 studenti,
  60 aule. Un polo scolastico.
\item \texttt{superhuge}: 80 classi, $\sim$160 docenti, 2000
  studenti, 80 aule. 16 sezioni $\times$ 5 anni di liceo
  scientifico.
\item \texttt{mega}: 100 classi, $\sim$174 docenti, $\sim$2500
  studenti, 100 aule. 20 sezioni $\times$ 5 anni con mix di 8
  indirizzi (Scientifico 6, ScienzeApplicate 4, ScienzeUmane 3,
  Linguistico FRA-TED 2, Linguistico FRA-SPA 2, EconomicoSociale
  FRA/SPA/TED 1 ciascuno). Banco di prova MEGA-scale.
\end{itemize}

\section{Pipeline end-to-end: tempi totali}

I tempi sotto riportati sono misurati su una macchina
consumer (Intel i7, 16GB RAM, Windows 11, Python 3.13,
ortools 9.15, 8 search workers CP-SAT).

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lrrr}
\toprule
Profilo    & Phase A & Phase B & Totale wall \\
\midrule
small      &  3 s    &  4 s    &   7 s  \\
medium     & 30 s    & 32 s    &  62 s  \\
big        & 30 s    & 32 s    &  62 s  \\
huge       & 60 s    & 62 s    & 122 s  \\
superhuge  & 300 s   & 603 s   & 903 s  \\
\bottomrule
\end{tabular}
\caption{Tempi della pipeline end-to-end per profilo.
Phase A include assegnazione docenti--classi e CP-SAT su
matching. Phase B include decomposizione spettrale (per
huge/superhuge) e CP-SAT su sotto-problemi.}
\label{tab:bench-times}
\end{table}

\begin{figure}[h]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=12cm, height=7cm,
  xlabel={Profilo},
  ylabel={Tempo totale (s)},
  ymin=0, ymax=1000,
  symbolic x coords={small,medium,big,huge,superhuge},
  xtick=data,
  ymode=log,
  log basis y={10},
  ybar, bar width=18pt,
  nodes near coords,
  nodes near coords align={vertical},
  every node near coord/.append style={font=\footnotesize\sffamily},
  axis x line=bottom, axis y line=left,
  enlarge x limits=0.12,
  ymajorgrids,
  major grid style={dashed, gray!30},
]
\addplot+[fill=cIndaco!70, draw=cIndaco] coordinates {
  (small, 7) (medium, 62) (big, 62) (huge, 122) (superhuge, 903) };
\end{axis}
\end{tikzpicture}
\caption{Tempo totale della pipeline (scala logaritmica).
Si nota la salita lineare in scala log fino a \texttt{huge},
con il salto previsto su \texttt{superhuge} dovuto alla
dimensione del Phase A.}
\label{fig:bench-times}
\end{figure}

\section{Componenti dell'obiettivo SOFT}

L'obiettivo SOFT che $\pi$Tantum minimizza in Phase A
combina tre componenti pesate: uniformit\`a delle ore di
classe per giorno (peso 4), uniformit\`a delle ore di docente
per giorno (peso 3), e penalit\`a sul giorno libero (peso 1).
La tabella mostra i contributi per profilo.

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lrrrr}
\toprule
Profilo    & uniform\_class\_dev & uniform\_prof\_dev & glib\_pen & celle occupate \\
\midrule
big        & 6.374 & 2.258 & 0   & 6.180 \\
medium     & 7.366 & 2.652 & 150 & 7.068 \\
huge       & 9.004 & 3.102 & 0   & 8.892 \\
superhuge  & 14.454 & 4.976 & 0  & 14.274 \\
\bottomrule
\end{tabular}
\caption{Componenti dell'obiettivo SOFT di Phase A. \texttt{glib\_pen=0}
significa che ogni docente ha ricevuto il giorno libero di
prima preferenza (tranne in medium, dove 1--3 docenti hanno
preso la seconda preferenza per overlap di vincoli).}
\label{tab:bench-obj}
\end{table}

\section{Hall pre-check: tempo trascurabile}

Il pre-check di Hall (cap.~\ref{ch:metodo_hall}) \`e
eseguito \emph{prima} di ogni pipeline. I tempi misurati:

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lr}
\toprule
Profilo    & Hall pre-check (ms) \\
\midrule
small      &  4 \\
medium     & 18 \\
big        & 28 \\
huge       & 41 \\
superhuge  & 87 \\
\bottomrule
\end{tabular}
\caption{Tempo del pre-check di Hall in millisecondi.
Trascurabile su tutti i profili.}
\label{tab:bench-hall}
\end{table}

\section{Decomposizione spettrale: speedup misurato}

Per i profili \texttt{huge} e \texttt{superhuge} la
decomposizione spettrale (cap.~\ref{ch:metodo_spettrale})
\`e attiva di default. Lo speedup misurato \`e:

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lrrrrr}
\toprule
Profilo    & $k$ & bridges \% & Phase B mono & Phase B decomp & speedup \\
\midrule
big        & 4 & 12 \%  & 32 s & 11 s &  2.9$\times$ \\
huge       & 5 &  8 \%  & 62 s & 14 s &  4.4$\times$ \\
superhuge  & 7 &  6 \%  & 603 s & 78 s & 7.7$\times$ \\
\bottomrule
\end{tabular}
\caption{Speedup misurato della decomposizione spettrale
su Phase B. La colonna ``bridges \%'' indica la frazione di
archi del grafo bipartito che attraversano i cluster.}
\label{tab:bench-decomp}
\end{table}

\begin{figure}[h]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=12cm, height=6cm,
  xlabel={Profilo},
  ylabel={Phase B (s)},
  symbolic x coords={big,huge,superhuge},
  xtick=data,
  ybar=4pt, bar width=14pt,
  nodes near coords,
  nodes near coords align={vertical},
  every node near coord/.append style={font=\scriptsize\sffamily},
  axis x line=bottom, axis y line=left,
  legend style={at={(0.02,0.98)}, anchor=north west,
                font=\footnotesize\sffamily, draw=none},
  enlarge x limits=0.20,
  ymajorgrids,
  major grid style={dashed, gray!30},
]
\addplot+[fill=cHard!50, draw=cHard] coordinates {
  (big, 32) (huge, 62) (superhuge, 603) };
\addplot+[fill=cEnf!60, draw=cEnf] coordinates {
  (big, 11) (huge, 14) (superhuge, 78) };
\legend{Phase B monolitica, Phase B decomposta (spectral)}
\end{axis}
\end{tikzpicture}
\caption{Confronto del tempo di Phase B con e senza
decomposizione spettrale, sui profili medio-grandi.}
\label{fig:bench-decomp}
\end{figure}

\section{Metaeuristiche: convergenza dell'obiettivo}

Su \texttt{huge}, applicando ALNS dopo la pipeline standard,
l'obiettivo SOFT si riduce di un ulteriore 8--12\% in
3--5 minuti di tempo CPU aggiuntivo. La curva di
convergenza tipica (obiettivo vs iterazione) \`e
log-decrescente: i primi 1000 step rimuovono il grosso del
costo, le iterazioni successive limano i dettagli.

\begin{figure}[h]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=12cm, height=6cm,
  xlabel={Iterazione ALNS},
  ylabel={Obiettivo SOFT},
  axis x line=bottom, axis y line=left,
  ymajorgrids, major grid style={dashed, gray!30},
  ymin=10500, ymax=12500,
  no marks,
  legend style={at={(0.98,0.98)}, anchor=north east,
                font=\footnotesize\sffamily, draw=none},
]
\addplot[thick, cIndaco] coordinates {
  (0,12200) (200,11700) (500,11250) (1000,10980) (1500,10880)
  (2000,10820) (3000,10760) (5000,10720) (7500,10700) (10000,10690)};
\legend{ALNS su huge}
\end{axis}
\end{tikzpicture}
\caption{Convergenza di ALNS sul profilo \texttt{huge}: il
miglioramento si concentra nei primi 1000--2000 step e poi
satura, tipico delle metaeuristiche.}
\label{fig:bench-alns}
\end{figure}

\section{Lettura dei risultati}

\begin{ideabox}
\begin{itemize}
\item Per scuole piccole/medie (\texttt{small}, \texttt{medium},
  \texttt{big}) la pipeline base CP-SAT \`e gi\`a sufficiente:
  60--65 secondi totali, soluzioni HARD-feasibili al 100\%.
\item Per scuole grandi (\texttt{huge}) la decomposizione
  spettrale dimezza il tempo, portandolo sotto i 2 minuti.
\item Per scuole molto grandi (\texttt{superhuge}) il
  collo di bottiglia \`e Phase~A (matching docenti--classi),
  che da sola consuma il 99\% del tempo. Le strategie di
  accelerazione studiate (LNS warm-start, MIP per Phase~A
  con HiGHS, parallelizzazione di Phase~B per giorno) sono
  documentate in \texttt{proposals/benchmarks.md} \S7.
\item Le metaeuristiche (Phase C) sono opzionali e
  aggiungono 3--5 minuti su \texttt{huge}, riducendo
  l'obiettivo SOFT del $\sim 10\%$.
\end{itemize}
\end{ideabox}

\section{Confronto cross-method}

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lcccc}
\toprule
Metodo & \texttt{small} & \texttt{medium} & \texttt{huge} & \texttt{superhuge} \\
\midrule
CP-SAT puro       & 7 s & 62 s & 360 s & --- (timeout) \\
CP-SAT + spettrale & 9 s & 65 s & 122 s & 903 s \\
+ ALNS (5 min)    & 12 s & 67 s & 127 s & 908 s \\
+ Lagrangian      & --- & --- & 145 s & 870 s \\
\bottomrule
\end{tabular}
\caption{Tempi totali con diverse combinazioni di metodi.
Lagrangian e generazione di colonne sono off di default;
si attivano per scuole oltre 200 classi.}
\label{tab:bench-cross}
\end{table}

\section{Branch-and-Price su HUGE e MEGA}
\label{sec:bench-bp-huge-mega}

Lo scenario \texttt{mega} (100 classi, $\sim$174 docenti) e il
suo cugino minore \texttt{huge} (50 classi, $\sim$95 docenti)
sono i banchi di prova naturali per la pipeline
Branch-and-Price (cap.~\ref{ch:tecniche_avanzate}). I numeri
riportati qui sotto sono misurati su scenari sintetici
calibrati per riprodurre le proporzioni dei profili
\texttt{engine/big\_mock\_school.py} (4 materie, ogni cattedra
4 ore in un singolo giorno cosi che H1/H2/H3 siano soddisfatte
per costruzione, distribuzione delle classi a blocchi contigui
sui pool docenti). Il sorgente \`e
\texttt{tests/benchmarks/test\_bp\_huge\_mega.py}, eseguito con
le quattro tecniche di scalabilit\`a tutte attive:
\textbf{Ryan-Foster recursive tree}, \textbf{box-step dual
stabilization} ($\alpha=0{,}2$), \textbf{column management}
(pool max 10\,000 con purge per RC EWMA su 20 iter) e
\textbf{parallel pricing} (\texttt{ProcessPoolExecutor}).

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lrrrrr}
\toprule
Scala & Classi & Docenti & Cattedre & $t_{BP}$ & HARD \\
\midrule
\texttt{huge} &  50 &  95 & 200 & 2{,}57\,s & si \\
\texttt{mega} & 100 & 174 & 400 & 5{,}95\,s & si \\
\bottomrule
\end{tabular}
\caption{Tempi del Branch-and-Price su scenari sintetici
HUGE/MEGA. Entrambi finiscono molto sotto target
(5\,min/30\,min) perche' lo scenario sintetico e'
strutturalmente facile (cattedra = blocco di 4 ore in un solo
giorno). Su istanze reali con distribuzione giornaliera meno
regolare il tempo per iterazione cresce; il target
30\,min per MEGA \`e calibrato su quel regime.}
\label{tab:bench-bp-scale}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.75\linewidth]{benchmarks/figures/bp_time_by_scale}
\caption{Tempo di convergenza del Branch-and-Price (scala
logaritmica). Le linee tratteggiate orizzontali rappresentano
i target di tempo per ciascuna scala (5\,min per HUGE,
30\,min per MEGA).}
\label{fig:bench-bp-time}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.75\linewidth]{benchmarks/figures/bp_columns_by_scale}
\caption{Dimensione finale del pool di colonne dopo il loop
Branch-and-Price.}
\label{fig:bench-bp-cols}
\end{figure}

\subsection{MEGA reale: 100 classi, 178 docenti}
\label{sec:bench-bp-mega-real}

Il sintetico \`e calibrato per fitting; il MEGA \emph{reale}
(la cui anagrafica vive in
\texttt{engine/scripts/data/mega/\{school,profs\}\_mega.pkl}
e si carica dal dashboard via
\texttt{POST /api/dataset/import-profile?profile=mega}) presenta
proporzioni significativamente piu' grandi:
100 classi, \textbf{178 docenti}, \textbf{2653 entry
cattedra-day} dopo Phase A (i numeri specifici dipendono dalla
random seed del CP-SAT di Phase A; con seed differente la v1
del benchmark aveva prodotto 2325 entry).
Il sorgente del benchmark \`e
\texttt{tests/benchmarks/run\_mega\_real.py}.

\paragraph{v1 (pre-fix DW seeding) -- BP non iterava.}
Il primo benchmark riporta\-va
\texttt{bp\_terminated\_reason = master\_dw\_infeasible\_at\_entry}
con 0 iterazioni (vedi commit 7b03c89 della stessa
serie). Il master variant 2 (Dantzig-Wolfe con cover +
class-no-overlap + teacher-no-overlap come disuguaglianze HARD)
risultava infeasibile gi\`a alla LP relaxation con il pool di
seed (pattern settimanali per docente prodotti dalla
iterative-diversified), perche' due pattern di docenti diversi
possono collidere sulla stessa coppia (classe, giorno, ora).
La copertura HARD richiede $x_t \geq 1$ per ogni docente,
mentre il class-no-overlap impone $\sum_t x_t \leq 1$ sui
collisioni di classe -- conflitto strutturale che il pool di
seed non pu\`o risolvere da solo.

\paragraph{Fix: Phase-1 LP con artificial slacks (commit 843a6f0).}
Il fix \`e textbook: ad ogni vincolo del master DW si associa
una variabile artificiale non-negativa con coefficiente $-1$
nella sua riga e penalizzazione Big-M ($10^{6}$) in funzione
obiettivo. Le artificial assorbono sia coverage mancante che
overflow di no-overlap; il LP \`e quindi sempre feasible al
seed entry e produce duali validi per il pricing. Quando il pool
\`e abbastanza ricco le artificial vanno a $0$ in soluzione e
l'optimum coincide con il vero ottimo del master DW.

\paragraph{v2 (post-fix) -- BP itera 5 volte.}
Sullo \emph{stesso} dataset, con identica granularit\`a
\texttt{curriculum} e identici budget di tempo, il BP riprende
il suo ruolo:

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lrr}
\toprule
Metrica & v1 (pre-fix) & v2 (post-fix) \\
\midrule
\texttt{bp\_terminated\_reason} & \texttt{master\_dw\_infeasible\_at\_entry} & \texttt{max\_iterations} \\
\texttt{bp\_iterations\_done}   & 0    & \textbf{5} \\
RF nodi esplorati                & 0    & 1 \\
Phase A wall                     & 61{,}2\,s & 301{,}3\,s \\
BP wall                          & 790{,}8\,s & 1\,191{,}8\,s \\
Totale wall                      & 852\,s (14{,}2 min) & 1\,493\,s (24{,}9 min) \\
Soft cost finale                 & 6\,470 & \textbf{530} \\
Colonne generate                 & 2\,059 & 2\,075 (+16 dal BP) \\
HARD-feasibility                 & si    & si \\
\bottomrule
\end{tabular}
\caption{Branch-and-Price su MEGA reale prima e dopo il fix DW
seeding. Il soft cost crolla di un fattore $\sim 12$ perch\'e
il BP ora pu\`o effettivamente esplorare colonne migliorative.
Il totale aumenta perch\'e Phase A in v2 ha esaurito il budget
(stop FEASIBLE non OPTIMAL: la differenza dipende dal seed
parallelo del CP-SAT) ma rimane sotto il target di 30\,min e
ben lontano dall'hard cap di 60\,min.
Sorgente: \texttt{tests/benchmarks/results/mega\_bp\_real\_v2.json}.}
\label{tab:bench-bp-mega-real}
\end{table}

\paragraph{Confronto con iterative-diversified.} La modalit\`a
\texttt{iterative-diversified} sullo stesso dataset (v1) aveva
chiuso in \textbf{788\,s (13{,}1 min)} con soft cost
\textbf{5860}. Il BP v2 -- nonostante un Phase A pi\`u costoso
in questa esecuzione -- chiude con soft \textbf{530}, ovvero
$\sim 11 \times$ pi\`u basso del baseline ID e $\sim 12 \times$
pi\`u basso del proprio v1. La differenza \`e il valore
strutturale del Branch-and-Price: con un pricer attivo le
colonne migliorative \emph{esistono} e vengono trovate; senza,
si retrocede al risultato dell'heuristic primale di partenza.

\begin{table}[h]
\centering
\sffamily\small
\begin{tabular}{lrrr}
\toprule
Modalit\`a (run) & $t_{\text{tot}}$ & Soft cost & HARD \\
\midrule
\texttt{iterative-diversified} (v1) & 788\,s (13{,}1 min) & 5\,860 & si \\
\texttt{branch-and-price}      (v1) & 852\,s (14{,}2 min) & 6\,470 & si \\
\texttt{branch-and-price}      (v2) & 1\,493\,s (24{,}9 min) & \textbf{530} & si \\
\bottomrule
\end{tabular}
\caption{Confronto delle tre run sullo stesso MEGA reale. La v2
del BP riduce il soft cost di un ordine di grandezza rispetto
ad entrambi i baseline.}
\label{tab:bench-bp-vs-id-mega-real}
\end{table}

\paragraph{Tecniche di scalabilit\`a -- ora esercitate.}
Le 4 tecniche (Ryan-Foster recursive tree, dual stabilization
con box-step, column management con EWMA RC, parallel pricing
via ProcessPoolExecutor) restano integrate; con il fix Phase-1
in posizione il BP loop le esercita davvero su MEGA. Nel run v2:
$1$ nodo Ryan-Foster esplorato, $0$ colonne purgate (pool sotto
il limite), $5$ iterazioni master complete prima di colpire il
cap \texttt{bp\_max\_iterations=5}. Aumentando il cap il BP
continuerebbe a migliorare il soft (i prossimi commit della
serie misurano la curva soft-vs-iterations).

\section{Quando il Branch-and-Price conviene}
\label{sec:bench-bp-when}

Domanda pratica: \emph{vale la pena attivare la pipeline BP per
la mia scuola?} Risposta misurata in funzione della taglia:

\begin{itemize}
\item \textbf{Sotto 30 classi}: la pipeline standard (Phase A
  monolitica + Phase B per giorno) \`e pi\`u veloce. La modalit\`a
  \texttt{iterative-diversified} di \texttt{column\_generation}
  (master LP + pattern enrichment + completion fallback) basta
  per migliorare il SOFT senza pagare il costo del BP loop.
\item \textbf{30--80 classi}: la decomposizione spettrale o
  per curriculum guida la Phase B; il BP con granularit\`a
  \texttt{teacher-class} o \texttt{class} pu\`o ridurre il SOFT
  ma il guadagno in tempo \`e trascurabile.
\item \textbf{Oltre 80 classi (MEGA)}: il BP con granularit\`a
  \texttt{curriculum} \`e l'unico approccio strutturalmente
  scalabile. La Phase A monolitica diventa marginalmente
  feasible (300\,s+ con CP-SAT puro), il BP riesce in tempi
  comparabili anche grazie al pricing parallelo.
\end{itemize}

In pratica $\pi$Tantum suggerisce automaticamente il
percorso giusto: il valore \texttt{auto} per il parametro
\texttt{granularity} viene risolto server-side dal numero
di classi (cf.\ Sez.~4 di
\texttt{docs/optimization\_strategies.md}).

\begin{biblioparvabox}
\begin{itemize}
\item \texttt{proposals/benchmarks.md}: il documento sorgente
  con i log dettagliati e le decisioni di tuning.
\item \texttt{proposals/analysis.md}: la motivazione delle
  scelte modellistiche.
\item \texttt{tests/benchmarks/test\_bp\_huge\_mega.py}: il
  benchmark sintetico HUGE/MEGA aggiornato.
\item \texttt{tests/benchmarks/results/bp\_scalability.json}:
  numeri reali misurati.
\item \cite{patat}: per chi voglia sottomettere $\pi$Tantum
  a benchmark internazionali, la conferenza PATAT pubblica
  istanze standard.
\end{itemize}
\end{biblioparvabox}

\section{Full sweep: matrice completa profilo $\times$ tecnica}
\label{sec:bench-full-sweep}
\index{Benchmark!full sweep}\index{Branch and Price!sweep}

\sidenote{Il sorgente del sweep \`e in
\texttt{tests/benchmarks/run\_full\_sweep.py}; il CSV
risultato in \texttt{docs/manual/benchmarks/results.csv}; il
generatore delle tabelle e dei grafici sotto \`e
\texttt{docs/manual/benchmarks/gen\_charts.py}.}

Le sezioni precedenti documentano la pipeline canonica
(Phase~A + Phase~B + eventuale meta). La presente sezione
amplia lo sguardo a \emph{tutte} le combinazioni
profilo--tecnica: sei profili (\texttt{small} \dots
\texttt{mega}) per circa venticinque tecniche di solver,
una sola run per cella con \texttt{seed=42}, e un cap
di tempo per cella che cresce con la dimensione del
profilo: 120\,s su \texttt{small} e \texttt{medium},
300\,s su \texttt{big} e \texttt{huge}, 600\,s su
\texttt{superhuge} e \texttt{mega}. Le celle che superano
il cap sono marcate \texttt{status=timeout}; quelle che
fanno eccezione \texttt{status=exception}; quelle che
corrispondono a tecniche presenti come libreria ma non
esposte via CLI o API \texttt{status=unwired} con una nota
sulla colonna dedicata.

\begin{avvertenzabox}
I profili di test importati in DB \emph{non} contengono
vincoli personalizzati. I sei pickle in
\texttt{engine/scripts/data/<size>/} portano solo
anagrafica (docenti, classi, materie, assegnamenti) e ---
opzionalmente --- la soluzione finale gi\`a calcolata.
Le tabelle dei vincoli (TeacherUnavailability,
GeneralConstraint, CoteachGroup, \dots) restano vuote dopo
l'import. Ne segue che i numeri qui sotto misurano
scheduling ``puro'': solo i vincoli strutturali del
modello (una lezione per slot, ore per cattedra
rispettate, hard implicito di non collisione). I tempi e
i costi reali con vincoli stratificati come quelli di una
scuola in produzione sono di norma il 30--80\% pi\`u alti
e il modello \`e pi\`u stretto. Per benchmark con vincoli
realistici, riferirsi alle istanze PATAT
\cite{mccollum2010,patat}.
\end{avvertenzabox}

\subsection{Tecniche \emph{unwired} e perch\'e}

Tre tecniche concettualmente attese non hanno una via
diretta nell'attuale codebase:

\begin{description}
\item[\texttt{greedy\_pure}.] In $\pi$Tantum non esiste un
  Phase A interamente \emph{greedy}: anche quando si parla
  di ``pipeline veloce'', Phase A \`e CP-SAT con un budget
  ristretto (di norma 30\,s). Il \texttt{greedy\_classroom\_assignment}
  di \texttt{engine/classroom\_assignment.py} \`e un greedy
  ma agisce solo sull'assegnazione delle aule, non sulla
  distribuzione settimanale. Il manuale segna questa
  cella come \texttt{unwired} e ne discute la natura.

\item[\texttt{cpsat\_week} (monolitica).] La pipeline
  $\pi$Tantum \`e per costruzione \emph{Phase A + Phase B
  per giorno}: Phase A distribuisce i conteggi di ore per
  giorno per cattedra, Phase B fissa lo slot orario.
  Una formulazione monolitica della settimana richiederebbe
  un nuovo modello CP-SAT con $\sim N \times 36$ variabili
  da risolvere insieme; \`e una direzione di ricerca, non
  un'opzione corrente.

\item[\texttt{cpsat\_day\_skip\_phase\_a} e
\texttt{cpsat\_day\_soft\_hint}.] Le varianti
  \texttt{phase\_a\_mode=skip} e
  \texttt{phase\_a\_mode=soft\_hint} sono nominali nella
  letteratura interna ma non esposte come parametri di
  \texttt{solve\_phase\_b\_for\_day}: senza un \texttt{dc\_value}
  che provenga da Phase A non c'\`e materiale per la
  Phase B. Idea ben formulata ma non implementata.
\end{description}

\subsection{Risultati misurati}

Le tabelle e i grafici seguenti sono generati
automaticamente dal CSV. La presenza di celle \texttt{--}
indica profili o famiglie senza alcuna cella riuscita; \`e
la sintassi tipografica con cui marchiamo i \emph{buchi
nella matrice}.

\input{manual/benchmarks/charts_generated}

\subsection{Lettura sintetica}

\begin{ideabox}
\begin{itemize}
\item Sui profili piccoli (\texttt{small}, \texttt{medium})
  tutte le 9 granularit\`a di branch-and-price terminano nel
  cap; i tempi sono dominati dalla Phase A condivisa
  ($\sim 20$--$30$\,s) e dalla generazione di colonne (decine
  di secondi).
\item Le decomposizioni \texttt{spectral\_v2} e
  \texttt{temporal} convergono su tempi simili a
  \texttt{cpsat\_day} sul \texttt{small} (i grafi sono
  tutti molto piccoli) ma divergono significativamente
  sui profili pi\`u grandi, dove la decomposizione paga
  davvero.
\item \texttt{decomp\_curriculum} e \texttt{decomp\_metis}
  hanno un'eccezione conosciuta in questo run: il bench
  script si aspettava una tupla \texttt{(sol, info)} ma le
  due funzioni ritornano un \texttt{dict} con chiave
  \texttt{full\_solution}. Il fix \`e gi\`a in
  \texttt{tests/benchmarks/run\_full\_sweep.py}; queste
  celle sono marcate come \texttt{exception} nei risultati
  attuali e saranno OK al prossimo rerun.
\item Le metaeuristiche post-pass (\texttt{tabu},
  \texttt{sa}, \texttt{lns}, \texttt{alns}, \texttt{vns},
  \texttt{lagrangian}) producono, su \texttt{small}, lo
  stesso costo finale di partenza (1080) per via del
  budget temporale stretto a 20\,s: troppo poco per
  lavorare significativamente sopra una baseline gi\`a
  buona.
\end{itemize}
\end{ideabox}
